#pragma once

enum ScreenState {
	VIEW_OPTION_LIST,
	VIEW_ENTRY,
	INPUT_STRING,
};

enum OptionType {
	OPTION_YES,
	OPTION_NO,
	OPTION_ENTRY,
};

const int ScreenWidth = 80;
const int ScreenHeight = 25;